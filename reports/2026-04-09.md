# Standup Report — 2026-04-09

## 1. What Was Done

- **#17 Store brochure viewer** — PR #43 merged. Full-stack implementation:
  - Backend: `Brochure` model + Alembic migration 0003; 4 REST endpoints under `/stores`
  - Frontend: `/stores` index page (store cards with brochure badge + validity dates)
  - Frontend: `/stores/[id]/brochure` viewer (iframe PDF embed, download link, past brochures list)
  - `BrochurePdfViewer` component — mobile-friendly (4:3 ratio, native pinch-to-zoom)
  - Product deep-link: "Search products from store" button

## 2. In Progress

- None.

## 3. Blockers

- None.

## 4. Product Strategist Input

The initial backlog is now **100% complete**. The platform is functionally end-to-end:
scraping → parsing → catalogue → comparison → history → brochure viewer.

Suggested next sprint candidates (PO-suggestion — require Boris approval before work starts):

| # | Idea | Value | Effort |
|---|------|-------|--------|
| A | **Price alerts** — notify user when a tracked product drops below a threshold | High (retention + engagement) | M |
| B | **Docker Compose dev environment** — single `docker compose up` for local dev | High (DX, onboarding) | S |
| C | **Brochure product linkage** — store parsed brochure items in DB and deep-link to catalogue entries | Medium (connects brochure viewer to catalogue) | M |
| D | **CI/CD deploy pipeline** — GitHub Actions → staging on merge to main | High (production readiness) | M |
| E | **User accounts + shopping list** — save products, share basket comparison | Medium (long-term retention) | L |

Recommendation: **B** (Docker Compose) unblocks contributors immediately at zero user risk.
**D** (CI/CD) should follow to close the delivery loop. Both fit in one sprint.

## 5. Stakeholder Actions Needed (Boris)

1. **Merge decision — PDF renderer**: PR #43 brochure viewer uses `<iframe>`.
   Alternative: `react-pdf` for page control + text selection (+600 kB bundle).
   Please confirm: keep `<iframe>` or switch to `react-pdf`.

2. **Close done issues** — the following issues are labeled `done` and remain open.
   Boris to close (agents cannot close issues per team rules):
   `#3, #4, #9, #10, #11, #12, #15, #16, #18`

3. **Approve next sprint scope** — choose from suggestions A–E above or provide new priorities.
