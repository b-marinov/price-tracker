# Daily Standup Report — 2026-04-15

## What Was Done

### Issue: Show pack variants on product page (Бял хляб 1кг vs 1 пакет)
- **Backend models** (`app/models/product.py`, `app/models/price.py`):
  - Added `generic_pack` and `pack_type` columns to Product and Price models
  - `pack_type`: packaging material (кенче, пакет, бутилка, etc.)
  - `generic_pack`: size/quantity (1 кг, 500 г, etc.)
  - `pack_info`: computed string for backward compatibility

- **Matching logic** (`app/scrapers/matching.py`):
  - Updated `_match_by_sku()` to match on name + brand + generic_pack only
  - Different pack_types now create variants of same product family
  - Added `_extract_pack_components()` to parse full pack_info into generic_pack + pack_type

- **LLM parser** (`app/scrapers/llm_parser.py`):
  - Added `pack_type` field to `LLMBrochureItem`
  - Updated system prompt to request pack_type extraction
  - Updated `llm_items_to_scraped()` to pass pack_type through pipeline

- **Pipeline** (`app/scrapers/pipeline.py`):
  - Updated `find_or_create_product()` call to handle pack_type
  - Updated Price creation to store pack_type

- **Schemas** (`app/schemas/catalogue.py`):
  - Added `generic_pack` and `pack_type` to ProductDetail and StorePriceSummary
  - Added new `_enrich_product_detail()` helper function

- **Database migration**:
  - Created `0016_pack_type_generic_pack.py` migration

- **Frontend** (`frontend/app/products/[id]/page.tsx`):
  - Updated to display pack_type as badge on each price row
  - Shows full pack_info (generic_pack + pack_type) in the packaging column

## What Is In Progress

None

## Blockers

None

## Product Strategist Input

Pack variants allow:
- Same product (e.g., "Бял хляб") with different packaging (1кг vs 1 пакет) to appear together on one product page
- Clear distinction of packaging material via badge
- Users can see which stores have which variant at a glance

## Stakeholder Actions Needed

None pending

---

TOKEN_REPORT: actual=2000 remaining_session=77000
