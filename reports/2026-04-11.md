# Daily Standup — 2026-04-11 (Sessions 11 & 12)

## 1. What Was Done

### Shipped — commit `5d1195a` pushed to `main`

**GenericBrochureScraper** (replaces Billa, Kaufland, Lidl, Fantastico scrapers)
- Single scraper class with 3-strategy PDF URL discovery:
  1. Direct DOM `<a href="*.pdf">` link extraction via Playwright
  2. LLM text analysis of all page links (Gemma 4 text mode)
  3. Vision fallback — page screenshot → Gemma 4 identifies download buttons
- Store config in DB (`Store.brochure_url`), no code change needed for new stores
- Migration 0007 adds column and seeds all 4 store URLs

**Product naming fix**
- LLM prompt: `name` = generic product type only (e.g. "Олио"), brand stored separately
- Enables real cross-brand price comparison (Ferrero vs Milka both → "Шоколадови бонбони")

**Dynamic category filter chips** (frontend)
- Removed 29-entry hardcoded `CATEGORY_TAXONOMY` that didn't match backend names
- Chips now render from live backend categories

**QA core test coverage** (issue #70 — closed)
- `tests/test_pipeline.py` — 16 tests, in-memory aiosqlite, no PostgreSQL required
- `tests/routers/test_catalogue.py` — 15 tests for GET /products and /products/search
- `tests/routers/test_browse.py` — 13 tests for GET /browse and /browse/deals
- `tests/scrapers/test_generic_brochure.py` — 19 tests for GenericBrochureScraper

**CI fixes**
- Ruff: 0 errors — UP042 (StrEnum), SIM108 (ternary), B008 (noqa), E501/SIM117 (per-file-ignores)
- Mypy: 0 errors — removed unused type: ignore comments
- All 63 affected tests passing

## 2. What Is In Progress

Nothing actively in progress. Sprint closed.

## 3. Blockers

None.

## 4. Product Strategist Notes

- Generic scraper architecture means new stores cost ~5 minutes (DB seed only) vs. ~1 sprint per store. Boris-approved ToS gate (#67) should be implemented before any new store is added.
- Cross-brand comparison is now architecturally possible but historical data still has brand names embedded in `product.name` — cleanup migration (#69) is needed before this value shows up for users.

## 5. Stakeholder Actions Needed

All three items below were approved by Boris on 2026-04-11. No further approval needed — ready to implement in next sprint.

| # | Issue | Size | What it does |
|---|---|---|---|
| #67 | ToS gate for new stores | S | Checklist/approval step before seeding brochure_url for any new store |
| #68 | Scrape health alert (0 items) | S | Alert when a scraper run returns 0 items (silent failure detection) |
| #69 | Historical data cleanup | M | Migration to strip brand names from existing `product.name` values |

**Suggested next sprint order**: #68 → #67 → #69 → add T-Market or Penny to validate generic scraper.
